import { Box } from '@material-ui/core';
import styled from 'styled-components';

export const RangeInputWrapper = styled(Box)``;

export const Title = styled.h3`
  text-align: center;
  margin-bottom: 40px;
`;
